[Unit]
Description=Load bpctl_mod / rb_bpwatcher modules
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'if ! lsmod | grep -q bpc; then /usr/bin/bpctl_start; /sbin/modprobe rb_bpwatcher || exit 1; fi'
RemainAfterExit=true

[Install]
WantedBy=multi-user.target
